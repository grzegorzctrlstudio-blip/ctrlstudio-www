/** Shared easing — typed as a cubic-bezier tuple so it satisfies Motion's
 *  `Easing` type inside `Variants` constants (a bare number[] is widened). */
export const EASE_OUT: [number, number, number, number] = [0.16, 1, 0.3, 1];
